# 程式碼
```
<%
String adostr = "mysql,org.git.mm.mysql.Driver,jdbc:mysql://localhost/db1129?user=root&password=passwd1234";
util.DBConnection conn1 = new DBConnection(adostr); //資料庫連線
util.RecordSet rs1 = new util.RecordSet();

String sql = "select * from person"
rs1.Open(conn1,sql);
while (!rs1.EOF){
		System.out.println(rs1.get("pno"));
		rs1.next();
}
conn1.close();

%>
```
